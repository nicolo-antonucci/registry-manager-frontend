import { GetSetLengthPipe } from './get-set-length.pipe';

describe('SetLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSetLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
