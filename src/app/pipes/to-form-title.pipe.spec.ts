import { ToFormTitlePipe } from './to-form-title.pipe';

describe('ToFormTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ToFormTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
